rules_version='2'

service cloud.firestore {
  match /databases/{database}/documents {
    // All access is via Admin SDK (server-side only).
    // Client SDK access is denied entirely.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
